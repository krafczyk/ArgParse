#ifndef ARGPARSE_CONFIG_HDR
#define ARGPARSE_CONFIG_HDR

#cmakedefine ARGPARSE_HAVE_NULLPTR

#ifdef ARGPARSE_HAVE_NULLPTR
	#define ARGPARSE_NULLPTR nullptr
#else
	#define ARGPARSE_NULLPTR NULL
#endif

#endif
